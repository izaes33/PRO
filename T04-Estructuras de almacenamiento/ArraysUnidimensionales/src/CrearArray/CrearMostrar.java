package CrearArray;

import java.util.Arrays;

public class CrearMostrar {
    public void crearMostrar(){
        String[] frutas = new String[]{"Manzana", "Plátano", "Naranja", "Fresa", "Uva"};
        System.out.println(Arrays.toString(frutas));
        /* Para imprimir una lista o arreglo de forma legible en Java Usando un Arreglo (Array)
        se debe usar el método Arrays.toString() para que se imprima correctamente y no solo la dirección de memoria. */
    }
}
